# 저장소 표준화: 모든 텍스트는 LF 로 저장(크로스플랫폼·Linux CI 안전).
* text=auto eol=lf

# Windows 배치 스크립트는 CRLF 유지.
*.bat text eol=crlf
*.cmd text eol=crlf

# gradlew 셸 스크립트는 반드시 LF (Linux 러너에서 실행).
gradlew text eol=lf

# 바이너리.
*.jar binary
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
