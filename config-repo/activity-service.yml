# activity-service 전용 설정 (공통 application.yml 위로 병합)
server:
  port: 18802

spring:
  datasource:
    url: ${DB_URL:jdbc:h2:mem:rally_activity;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
