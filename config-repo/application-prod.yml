# =============================================================================
# prod 프로파일 공통 오버라이드 (홈서버 배포)
# 클라이언트가 spring.profiles.active=prod 로 뜨면 config-server 가 application.yml 위에 이 파일을 병합한다.
# 시크릿/자격증명은 여기 두지 말고 deploy/.env 환경변수로 주입한다(public repo).
# =============================================================================

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://210.121.177.150:18861/eureka/}
