# 실설정은 전부 config-server(config-repo/)에서 가져온다 — 여기엔 식별자 + config-server 주소만 둔다.
# config-server 주소는 부트스트랩이라 환경변수로 분리: dev=localhost(기본) / prod=CONFIG_SERVER_URL 주입.
spring:
  application:
    name: gateway
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:18859}
